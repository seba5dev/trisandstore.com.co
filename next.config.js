/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    //enter the domain or subdomain where you have WordPress installed
    domains: ["trisandstore.com.co"],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
